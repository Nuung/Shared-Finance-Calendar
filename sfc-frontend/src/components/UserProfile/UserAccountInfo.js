import React from "react";
import { Link } from "react-router-dom";

const UserAccountInfo = ({ type }) => {
    return (
        <div className="profile__div"></div>
    );
};

export default UserAccountInfo;
